# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Binary
dist

# VSCode
.vscode

# Local credentials: accessKey / secretKey / bucket must never be committed.
# Use example-config.yaml as the template.
config.yaml
config.local.yaml
*.faust.yaml
